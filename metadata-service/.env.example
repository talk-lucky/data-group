# Server Configuration
PORT=8080

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=metadata_db
# DB_SSLMODE can be set to "disable", "require", "verify-ca", "verify-full"
DB_SSLMODE=disable

# Gin Mode (debug, release, test)
GIN_MODE=debug
